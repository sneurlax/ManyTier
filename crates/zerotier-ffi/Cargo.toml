[package]
name = "zerotier-ffi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "C ABI bindings for the ZeroTier node engine"

[lib]
crate-type = ["cdylib"]

[dependencies]
zerotier-node = { workspace = true, features = ["native"] }
